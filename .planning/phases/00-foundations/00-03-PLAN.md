---
phase: 00-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - 00-02
files_modified:
  - rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt
  - rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackendSelfTest.kt
  - audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt
  - audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt
  - audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioSelfTest.kt
  - ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt
  - ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFakeSelfTest.kt
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
  - FOUND-05
must_haves:
  truths:
    - "FakeRigBackend exists at the same RigBackend interface boundary as DigirigRigBackend; supports simulateDetach(), configurable CAT response delay, configurable timeout-then-error, and configurable PTT-state observation."
    - "FakeUsbAudioCapture and FakeUsbAudioPlayback exist at the boundaries used by UsbAudioCapture / UsbAudioPlayback respectively; capture supports injectable PCM frames + configurable zero-sample return + simulated device removal; playback records every playBlocking call for assertion."
    - "Ft8DecoderFake exposes the surface of Ft8Native (isAvailable, version, decode, encode) and lets tests inject canned Ft8DecodeResult sequences without loading libft8vc.so on the JVM."
    - "All three fakes plus their self-tests compile and run via `./gradlew :rig:test :audio:test :ft8-native:test` (production code is unchanged)."
    - "Self-tests use Turbine for flow assertions and MockK only for platform-boundary helpers (no domain types mocked)."
  artifacts:
    - path: "rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt"
      provides: "FakeRigBackend class implementing RigBackend + CatControl; testable seam for PTT key/release, CAT freq/mode, simulated detach/timeout/zero-sample injection."
      min_lines: 80
      exports: ["FakeRigBackend"]
    - path: "audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt"
      provides: "FakeUsbAudioCapture (mirrors UsbAudioCapture: start/stop/onFrames callback) with frame injection + zero-sample mode + simulateDeviceRemoved()."
      min_lines: 60
      exports: ["FakeUsbAudioCapture"]
    - path: "audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt"
      provides: "FakeUsbAudioPlayback (mirrors UsbAudioPlayback.playBlocking + stop) recording every playBlocking invocation as PlaybackRecord(pcm, deviceId, halted)."
      min_lines: 40
      exports: ["FakeUsbAudioPlayback", "PlaybackRecord"]
    - path: "ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt"
      provides: "Ft8DecoderFake (object or class with same surface as Ft8Native: isAvailable, version, decode, encode) for JVM tests; canned-result queue + version-mismatch simulation."
      min_lines: 50
      exports: ["Ft8DecoderFake"]
  key_links:
    - from: "rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt"
      to: "rig/src/main/java/net/ft8vc/rig/RigBackend.kt"
      via: "Implements RigBackend interface (keyPtt, releasePtt)"
      pattern: "class FakeRigBackend.*: RigBackend"
    - from: "rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt"
      to: "rig/src/main/java/net/ft8vc/rig/CatControl.kt"
      via: "Implements CatControl interface (frequencyHz, setFrequencyHz, mode, setMode, catPtt) — DigirigRigBackend implements both, so the fake covers both surfaces."
      pattern: "CatControl"
    - from: "audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt"
      to: "audio/src/main/java/net/ft8vc/audio/AudioEngine.kt"
      via: "FakeUsbAudioCapture implements AudioEngine (the production interface UsbAudioCapture already implements), giving controllers a uniform seam."
      pattern: "AudioEngine"
---

<objective>
Author the three test fakes that PITFALLS Pitfall 10 names as Phase 0's hardest prerequisite: `FakeRigBackend` (FOUND-03), `FakeUsbAudio` capture+playback pair (FOUND-04), and `Ft8DecoderFake` (FOUND-05). Each fake lives in its module's `src/test/java/.../fakes/` subpackage, mirrors the boundary of the production sibling it stands in for, supports the failure-injection switches that Phase 1-5 controller tests will exercise, and ships with a minimal self-test that proves the failure switches work.

Purpose: Without these fakes, no Phase 1-5 controller can be unit-tested with the boundary discipline PITFALLS Pitfall 5 requires (mock only the platform boundary; never mock domain types). The fakes also feed the golden-trace harness (Plan 04).

Phase 0 constraint reminder: **no production code changes.** Per `<phase_specific_constraints>` option (b), each fake either implements an existing production interface (`RigBackend`, `CatControl`, `AudioEngine`) directly or — where production exposes a concrete class only — declares a local test-side Kotlin interface in the same test file and uses duck-typed binding. `UsbAudioCapture` already implements the existing `AudioEngine` interface (verified in audio/src/main/java/net/ft8vc/audio/AudioEngine.kt via the `outputSampleRateHz` + `start`/`stop` shape in UsbAudioCapture.kt:25-79), and `UsbAudioPlayback` is a concrete class with `playBlocking(samples12k, preferredDeviceId)` + `stop()` — the Playback fake declares a local interface `UsbAudioPlaybackFake` matching that shape and the fake implements it. `Ft8Native` is a Kotlin `object` (singleton) with no interface in production; the fake declares a `Ft8DecoderFake` interface (capturing the four members: `isAvailable`, `version`, `decode`, `encode`) and the fake implements it; controllers in later phases will take this interface as a constructor seam.

Output: Four fake source files + three self-test files in three modules' `src/test/` trees. No production source touched. Self-tests use Turbine 1.2.1 for any flow assertions, MockK only for synthetic platform-edge helpers (never for `QsoMachine`, `QsoMessages`, etc).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

@rig/src/main/java/net/ft8vc/rig/RigBackend.kt
@rig/src/main/java/net/ft8vc/rig/CatControl.kt
@rig/src/main/java/net/ft8vc/rig/DigirigRigBackend.kt
@rig/src/main/java/net/ft8vc/rig/Ft891Cat.kt
@audio/src/main/java/net/ft8vc/audio/AudioEngine.kt
@audio/src/main/java/net/ft8vc/audio/UsbAudioCapture.kt
@audio/src/main/java/net/ft8vc/audio/UsbAudioPlayback.kt
@ft8-native/src/main/java/net/ft8vc/ft8native/Ft8Native.kt
@ft8-native/src/main/java/net/ft8vc/ft8native/Ft8DecodeResult.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FakeRigBackend + FakeRigBackendSelfTest in rig/src/test/java/net/ft8vc/rig/fakes/</name>
  <files>rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt, rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackendSelfTest.kt</files>
  <read_first>
    - rig/src/main/java/net/ft8vc/rig/RigBackend.kt (production interface — keyPtt(), releasePtt(); the fake must implement this exact surface)
    - rig/src/main/java/net/ft8vc/rig/CatControl.kt (production interface — frequencyHz(), setFrequencyHz(Long): Boolean, mode(): Ft891Cat.Mode?, setMode(Ft891Cat.Mode): Boolean, catPtt(Boolean): Boolean)
    - rig/src/main/java/net/ft8vc/rig/DigirigRigBackend.kt (production type that implements both interfaces — read class declaration line and the keyPtt/releasePtt/frequencyHz/setFrequencyHz/mode/setMode/catPtt method signatures to mirror exactly)
    - rig/src/main/java/net/ft8vc/rig/Ft891Cat.kt lines 1-50 (Mode enum needed by the fake's mode()/setMode() API; MIN_FREQ_HZ + MAX_FREQ_HZ for the fake's setFrequencyHz validation)
    - .planning/research/PITFALLS.md Pitfall 5 (mock only the platform boundary — the self-test for the fake itself does NOT use Mockito/MockK; it asserts behavior directly)
  </read_first>
  <behavior>
    - Test 1 (keyPtt + releasePtt): after construct, pttKeyed == false; after keyPtt(), pttKeyed == true; after releasePtt(), pttKeyed == false; releasePtt() is idempotent (calling twice does not throw).
    - Test 2 (frequencyHz default + setFrequencyHz): default frequencyHz() returns the constructor default; setFrequencyHz(14_074_000L) returns true and subsequent frequencyHz() returns 14_074_000L.
    - Test 3 (mode default + setMode): default mode() returns Ft891Cat.Mode.DATA_USB; setMode(Ft891Cat.Mode.USB) returns true and subsequent mode() returns USB.
    - Test 4 (configureLatencyMs): with configureLatencyMs(50), frequencyHz() takes at least 45 ms to return (measured via System.nanoTime()); the delay is implemented via Thread.sleep so it works without coroutines in the self-test.
    - Test 5 (configureTimeoutHz + configureTimeoutMode): with configureTimeoutHz(true), frequencyHz() returns null on the next call; with configureTimeoutMode(true), mode() returns null on the next call.
    - Test 6 (simulateDetach): after simulateDetach(), every subsequent keyPtt/releasePtt records a detached event in the pttEdges history with kind == DETACHED, and frequencyHz()/mode() return null; reattach() restores normal operation.
    - Test 7 (pttEdges history): pttEdges is a thread-safe list (or an immutable snapshot accessor `pttEdgesSnapshot(): List<PttEdge>`) that records every keyPtt/releasePtt as a PttEdge data class with kind (KEY|RELEASE|DETACHED) and timestampMs (System.currentTimeMillis at call time). pttEdgesSnapshot() returns at least 4 entries after the test exercises 2 key + 2 release calls.
    - Test 8 (configureCatPttResponds): catPtt(true) and catPtt(false) return the configured value (default true); a separate counter increments on each call; assertable via catPttInvocations.
  </behavior>
  <action>
    Create rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt. Package: `net.ft8vc.rig.fakes`. Class declaration: `class FakeRigBackend(initialFrequencyHz: Long = 14_074_000L, initialMode: Ft891Cat.Mode = Ft891Cat.Mode.DATA_USB) : RigBackend, CatControl`.

    Required public API on the fake (mirror DigirigRigBackend's surface exactly for the RigBackend + CatControl methods; the additional fields are test-only switches):
    - `override fun keyPtt()` — flips a `@Volatile private var pttKeyed: Boolean` to true and appends a `PttEdge(kind = PttEdgeKind.KEY, timestampMs = clock.invoke())` to the synchronized history list. If `detached == true`, append `PttEdge(kind = PttEdgeKind.DETACHED, ...)` instead and skip the key.
    - `override fun releasePtt()` — flips pttKeyed to false and appends `PttEdge(kind = PttEdgeKind.RELEASE, ...)`. Idempotent (calling when already false still appends a RELEASE edge). If detached, append DETACHED edge.
    - `override fun frequencyHz(): Long?` — if detached → null; if `timeoutHz == true` → set timeoutHz=false and return null; otherwise apply `Thread.sleep(latencyMs)` if latencyMs > 0 and return `currentFrequencyHz`.
    - `override fun setFrequencyHz(hz: Long): Boolean` — if detached → false; if `hz !in Ft891Cat.MIN_FREQ_HZ..Ft891Cat.MAX_FREQ_HZ` → false; otherwise set `currentFrequencyHz = hz` and return true.
    - `override fun mode(): Ft891Cat.Mode?` — same pattern as frequencyHz(): detach → null; timeoutMode one-shot → null; otherwise return `currentMode`.
    - `override fun setMode(mode: Ft891Cat.Mode): Boolean` — if detached → false; otherwise set `currentMode = mode` and return true.
    - `override fun catPtt(on: Boolean): Boolean` — increment `catPttInvocations`; return configurable `catPttResponse` (default true). If detached → false.

    Test-only switches (all public, all `@Volatile` where read across threads):
    - `fun configureLatencyMs(ms: Long)` — sets latency applied to frequencyHz()/mode() (default 0).
    - `fun configureTimeoutHz(enable: Boolean)` — one-shot: next frequencyHz() returns null then resets.
    - `fun configureTimeoutMode(enable: Boolean)` — same pattern for mode().
    - `fun configureCatPttResponse(value: Boolean)` — controls catPtt() return value.
    - `fun simulateDetach()` / `fun reattach()` — toggle the `@Volatile private var detached: Boolean`.
    - `fun pttEdgesSnapshot(): List<PttEdge>` — defensive copy of the synchronized history.
    - `val pttKeyed: Boolean` (read-only property exposing the volatile flag).
    - `val catPttInvocations: Int` (volatile counter).
    - `val currentFrequencyHz: Long` (read-only accessor).
    - `val currentMode: Ft891Cat.Mode` (read-only accessor).
    - Optional ctor arg `clock: () -> Long = { System.currentTimeMillis() }` so the self-test can pin timestamps.

    Also in the same file: `enum class PttEdgeKind { KEY, RELEASE, DETACHED }` and `data class PttEdge(val kind: PttEdgeKind, val timestampMs: Long)`.

    All collections protected via `synchronized(historyLock)` where historyLock is a private Any() field. The fake is intended to be used from JVM unit tests only; cross-thread safety is required because some controllers will call from coroutine dispatchers different from the test thread.

    Then create rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackendSelfTest.kt. Package: `net.ft8vc.rig.fakes`. Top-level imports: JUnit 4 (`org.junit.Test`, `org.junit.Assert.*`), `net.ft8vc.rig.Ft891Cat`. One test class `FakeRigBackendSelfTest` with eight `@Test fun` methods, one per behavior case above. Use snake_or_camel test method names matching CONVENTIONS.md ("BDD-style: behavior_conditions"). Do NOT use MockK in this self-test; the fake is plain Kotlin and is asserted directly.

    Per CONVENTIONS.md: 4-space indent, no semicolons, Kotlin Official style, no wildcard imports beyond `org.junit.Assert.*` (which is the established pattern in core/src/test).
  </action>
  <verify>
    <automated>test -f rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt &amp;&amp; test -f rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackendSelfTest.kt</automated>
    <automated>grep -q '^package net\.ft8vc\.rig\.fakes' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt</automated>
    <automated>grep -q 'class FakeRigBackend' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt &amp;&amp; grep -q ': RigBackend, CatControl' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt</automated>
    <automated>grep -q 'fun simulateDetach' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt &amp;&amp; grep -q 'fun configureLatencyMs' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt &amp;&amp; grep -q 'fun configureTimeoutHz' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt</automated>
    <automated>grep -q 'enum class PttEdgeKind' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt &amp;&amp; grep -q 'data class PttEdge' rig/src/test/java/net/ft8vc/rig/fakes/FakeRigBackend.kt</automated>
    <automated>./gradlew :rig:test --tests "net.ft8vc.rig.fakes.FakeRigBackendSelfTest" --rerun-tasks</automated>
    <automated>! git diff --name-only HEAD rig/src/main/ 2>/dev/null | grep -q . ; echo "no production rig/ changes confirmed"</automated>
  </verify>
  <done>
    - FakeRigBackend compiles and implements RigBackend + CatControl.
    - Eight self-test cases pass via `./gradlew :rig:test --tests "*FakeRigBackendSelfTest"`.
    - simulateDetach + configureTimeoutHz + configureLatencyMs switches behave as specified.
    - pttEdgesSnapshot returns a defensive copy with correct edge ordering.
    - No file under rig/src/main/ was modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FakeUsbAudioCapture + FakeUsbAudioPlayback + shared self-test in audio/src/test/java/net/ft8vc/audio/fakes/</name>
  <files>audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt, audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt, audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioSelfTest.kt</files>
  <read_first>
    - audio/src/main/java/net/ft8vc/audio/AudioEngine.kt (production interface that UsbAudioCapture implements: `outputSampleRateHz: Int`, `fun start(preferredDeviceId: Int?, onFrames: (ShortArray) -> Unit)`, `fun stop()`. Mirror this exactly in FakeUsbAudioCapture.)
    - audio/src/main/java/net/ft8vc/audio/UsbAudioCapture.kt lines 25-90 (the start/stop lifecycle, thread model, frame-shape contract — frames are 16-bit mono PCM at 12 kHz)
    - audio/src/main/java/net/ft8vc/audio/UsbAudioPlayback.kt lines 19-79 (the playBlocking + stop surface; playBlocking returns Boolean (true if full payload transmitted, false if halted by stop). The fake must mirror this surface exactly even though no production interface exists — declare a local `UsbAudioPlaybackFake` interface in the fake's source file with `fun playBlocking(samples12k: ShortArray, preferredDeviceId: Int?): Boolean` and `fun stop()` so consumer controllers in Phase 5 can take the seam by interface.)
    - core/src/main/java/net/ft8vc/core/AppInfo.kt (read briefly — SAMPLE_RATE_HZ constant; the fake's outputSampleRateHz returns AppInfo.SAMPLE_RATE_HZ for parity)
    - .planning/research/PITFALLS.md Pitfall 8 (AudioRecord lifecycle on USB hot-swap — the fake's simulateDeviceRemoved() must support the zero-samples-returned scenario described there)
    - .planning/codebase/CONVENTIONS.md (file layout, naming, KDoc style)
  </read_first>
  <behavior>
    FakeUsbAudioCapture:
    - Test 1 (start with no frames): start(null, onFrames) sets isCapturing == true; no frames delivered until injectFrames() called; isCapturing == false after stop().
    - Test 2 (frame injection): injectFrames(shortArrayOf(1, 2, 3, 4)) while running → onFrames callback receives [1, 2, 3, 4] within 100 ms; subsequent injectFrames() append.
    - Test 3 (zero-sample mode): configureZeroSampleMode(true) → subsequent injectFrames() are discarded; capture continues "running" (isCapturing true) but onFrames is never invoked. Required for FOUND-04 + Pitfall 8 simulation.
    - Test 4 (simulateDeviceRemoved): simulateDeviceRemoved() flips internal deviceRemoved flag; the next injectFrames call is dropped silently AND deviceRemovedEvents counter increments AND isCapturing remains true (mirrors the Pitfall 8 scenario where AudioRecord.read returns 0 with no error code).
    - Test 5 (preferredDeviceId observation): start(deviceId=42, onFrames) records lastStartDeviceId == 42; assertable from the self-test.

    FakeUsbAudioPlayback:
    - Test 6 (playBlocking records call): playBlocking(shortArrayOf(1, 2, 3), deviceId=null) returns true; playbackRecords.size == 1; record contains pcm = [1,2,3], preferredDeviceId = null, halted = false.
    - Test 7 (stop halts in-flight playBlocking): configureBlockingMs(50) so playBlocking sleeps; call stop() from another thread after 10 ms; playBlocking returns false; the recorded entry has halted = true.
    - Test 8 (configureFailFastReturn): with configureFailFastReturn(true), playBlocking immediately returns false (halted=true) without sleeping — used to simulate the "throw inside playback callback" Pitfall 3 scenario without throwing on the test thread.
  </behavior>
  <action>
    Create audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt. Package: `net.ft8vc.audio.fakes`. Imports: `net.ft8vc.audio.AudioEngine`, `net.ft8vc.core.AppInfo`.

    Class: `class FakeUsbAudioCapture : AudioEngine`. Required members:
    - `override val outputSampleRateHz: Int = AppInfo.SAMPLE_RATE_HZ`
    - `@Volatile private var running: Boolean = false`
    - `@Volatile private var deviceRemoved: Boolean = false`
    - `@Volatile private var zeroSampleMode: Boolean = false`
    - `@Volatile private var deviceRemovedEvents: Int = 0`
    - `private var pendingOnFrames: ((ShortArray) -> Unit)? = null`
    - `@Volatile var lastStartDeviceId: Int? = null` (public, for assertion)
    - `val isCapturing: Boolean get() = running`
    - `override fun start(preferredDeviceId: Int?, onFrames: (ShortArray) -> Unit)` — record lastStartDeviceId, store onFrames, set running=true. Idempotent.
    - `override fun stop()` — clear pendingOnFrames, set running=false. Idempotent.
    - `fun injectFrames(frames: ShortArray)` — if !running: return; if deviceRemoved: increment deviceRemovedEvents and return; if zeroSampleMode: return; else invoke pendingOnFrames?.invoke(frames). All calls are synchronous (call on the test thread); the production type runs onFrames on the capture thread, but the fake collapses that for determinism.
    - `fun configureZeroSampleMode(enable: Boolean)` / `fun simulateDeviceRemoved()` / `fun reattachDevice()`. simulateDeviceRemoved flips deviceRemoved=true; reattachDevice flips false.
    - `val deviceRemovedEventCount: Int get() = deviceRemovedEvents`

    Create audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt. Package: `net.ft8vc.audio.fakes`.

    First declare a small Kotlin interface in the SAME file (above the class): `interface UsbAudioPlaybackFake { fun playBlocking(samples12k: ShortArray, preferredDeviceId: Int?): Boolean; fun stop() }`. This is the test-side seam Phase 5's TxOrchestrator will depend on — production `UsbAudioPlayback` does not implement it (no production changes allowed in Phase 0), but it duck-types: Phase 5 will introduce a `UsbAudioPlayback` interface in production source and `UsbAudioPlayback` (production class) will implement it then. For Phase 0, only the fake exists.

    Also in the same file: `data class PlaybackRecord(val pcm: ShortArray, val preferredDeviceId: Int?, val halted: Boolean)` (override equals/hashCode for ShortArray — generate `equals`/`hashCode` manually because ShortArray is referential).

    Class: `class FakeUsbAudioPlayback : UsbAudioPlaybackFake`. Required members:
    - `private val recordsLock = Any()`
    - `private val records = mutableListOf<PlaybackRecord>()`
    - `@Volatile private var blockingMs: Long = 0L`
    - `@Volatile private var failFast: Boolean = false`
    - `@Volatile private var halted: Boolean = false`
    - `override fun playBlocking(samples12k: ShortArray, preferredDeviceId: Int?): Boolean` — if failFast: synchronized(recordsLock) { records += PlaybackRecord(samples12k.copyOf(), preferredDeviceId, halted = true) }; return false. Else: halted = false; if blockingMs > 0: sleep in 10 ms ticks checking halted (so stop() can interrupt). After loop: synchronized(recordsLock) { records += PlaybackRecord(samples12k.copyOf(), preferredDeviceId, halted) }; return !halted.
    - `override fun stop()` — halted = true.
    - `fun playbackRecordsSnapshot(): List<PlaybackRecord> = synchronized(recordsLock) { records.toList() }`
    - `fun configureBlockingMs(ms: Long)` / `fun configureFailFastReturn(enable: Boolean)`

    Create audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioSelfTest.kt. Package: `net.ft8vc.audio.fakes`. JUnit 4 test class `FakeUsbAudioSelfTest` with eight `@Test` methods (one per behavior case). Use `runBlocking { ... }` only if you need delay; for thread joining in Test 7, use `Thread { Thread.sleep(10); fake.stop() }.also { it.start() }.join(200)`. Do NOT use MockK; the fakes are pure data + side effects.

    Per CONVENTIONS.md: 4-space indent, no semicolons.
  </action>
  <verify>
    <automated>test -f audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt &amp;&amp; test -f audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt &amp;&amp; test -f audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioSelfTest.kt</automated>
    <automated>grep -q 'class FakeUsbAudioCapture : AudioEngine' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt</automated>
    <automated>grep -q 'interface UsbAudioPlaybackFake' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt &amp;&amp; grep -q 'class FakeUsbAudioPlayback' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt &amp;&amp; grep -q 'data class PlaybackRecord' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt</automated>
    <automated>grep -q 'fun configureZeroSampleMode' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt &amp;&amp; grep -q 'fun simulateDeviceRemoved' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioCapture.kt</automated>
    <automated>grep -q 'fun configureBlockingMs' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt &amp;&amp; grep -q 'fun configureFailFastReturn' audio/src/test/java/net/ft8vc/audio/fakes/FakeUsbAudioPlayback.kt</automated>
    <automated>./gradlew :audio:test --tests "net.ft8vc.audio.fakes.FakeUsbAudioSelfTest" --rerun-tasks</automated>
    <automated>! git diff --name-only HEAD audio/src/main/ 2>/dev/null | grep -q . ; echo "no production audio/ changes confirmed"</automated>
  </verify>
  <done>
    - All three fake/test files compile.
    - Eight self-test cases pass via `./gradlew :audio:test --tests "*FakeUsbAudioSelfTest"`.
    - FakeUsbAudioCapture implements AudioEngine and supports injectFrames + configureZeroSampleMode + simulateDeviceRemoved.
    - FakeUsbAudioPlayback supports playbackRecordsSnapshot + configureBlockingMs + configureFailFastReturn.
    - UsbAudioPlaybackFake interface is declared in the playback file (Phase 5 controllers depend on it as a seam).
    - No file under audio/src/main/ was modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Ft8DecoderFake + Ft8DecoderFakeSelfTest in ft8-native/src/test/java/net/ft8vc/ft8native/fakes/</name>
  <files>ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt, ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFakeSelfTest.kt</files>
  <read_first>
    - ft8-native/src/main/java/net/ft8vc/ft8native/Ft8Native.kt (production object — public surface: `fun isAvailable(): Boolean`, `fun version(): String`, `fun decode(samples: ShortArray, sampleRate: Int = 12_000): Array<Ft8DecodeResult>`, `fun encode(message: String, freqHz: Float = 1000f, sampleRate: Int = 12_000): ShortArray`. Mirror this surface exactly via an interface in the fake.)
    - ft8-native/src/main/java/net/ft8vc/ft8native/Ft8DecodeResult.kt (the data class returned by decode — message: String, snr: Int, dtSeconds: Float, freqHz: Float, score: Int. Use as-is.)
    - .planning/research/PITFALLS.md Pitfall 5 (the fake exists so JVM tests can run without libft8vc.so loaded — verifies the isAvailable=false branch in production behaves as expected)
    - .planning/research/ARCHITECTURE.md §6 "Test Seam Interfaces" (sketch of Ft8Decoder interface; this plan implements that sketch as a test-side fake)
  </read_first>
  <behavior>
    - Test 1 (interface shape): Ft8DecoderFake.Companion can build an instance with default state — isAvailable() == true (default for tests), version() == "fake-1.0", decode(empty samples, 12000) == emptyArray(), encode("CQ TEST K1ABC", 1000f, 12000) returns ShortArray with size == 180_000 (15 seconds × 12_000 Hz).
    - Test 2 (queue canned decode results): queueDecodeResults(listOf(Ft8DecodeResult("CQ K1ABC FN42", -8, 0.5f, 1000f, 50))) → next decode() call returns those results; subsequent decode() returns empty (queue is one-shot per call).
    - Test 3 (sequence of canned results): queueDecodeResults can be called multiple times before a decode; the queue is FIFO per decode call (first queue → first decode, second queue → second decode).
    - Test 4 (decode invocation tracking): decode() invocations are recorded in decodeInvocations: List<DecodeInvocation>; each DecodeInvocation records sampleCount, sampleRate, returnedCount. Assertable from self-test.
    - Test 5 (configureIsAvailable false): configureIsAvailable(false) → isAvailable() returns false; decode() returns emptyArray() regardless of queued results; encode() returns empty ShortArray. Simulates the libft8vc.so-not-loaded case.
    - Test 6 (configureVersion): configureVersion("fake-2.5+abc123") → version() returns the configured string; configureVersion(null) → version() returns "not loaded" (matches production Ft8Native.version() behavior when !loaded).
    - Test 7 (encode behavior): default encode(message, freqHz, sampleRate) returns a ShortArray of size sampleRate * 15 filled with zeros (silence) — Phase 5 TX tests can swap in real-encoded PCM later by overriding via configureEncodeProducer.
    - Test 8 (configureEncodeProducer): configureEncodeProducer { msg, hz, sr -> shortArrayOf(1, 2, 3) } → next encode() returns [1, 2, 3]; encodeInvocations counter increments.
  </behavior>
  <action>
    Create ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt. Package: `net.ft8vc.ft8native.fakes`. Imports: `net.ft8vc.ft8native.Ft8DecodeResult`.

    Top of file declare the interface that consumers (Phase 3 DecodeController, Phase 5 TxOrchestrator) will take as a constructor seam: `interface Ft8DecoderApi { fun isAvailable(): Boolean; fun version(): String; fun decode(samples: ShortArray, sampleRate: Int = 12_000): Array<Ft8DecodeResult>; fun encode(message: String, freqHz: Float = 1000f, sampleRate: Int = 12_000): ShortArray }`. This interface mirrors Ft8Native's public surface exactly. Production Ft8Native is a Kotlin `object` and does not implement the interface (no production changes in Phase 0); Phase 3 or 5 will add a thin Ft8NativeAdapter in production source that bridges Ft8Native to Ft8DecoderApi.

    Class: `class Ft8DecoderFake : Ft8DecoderApi`. Required members:
    - `@Volatile private var available: Boolean = true`
    - `@Volatile private var version: String? = "fake-1.0"`
    - `private val queueLock = Any()`
    - `private val decodeQueue = ArrayDeque<Array<Ft8DecodeResult>>()`
    - `private val invocations = mutableListOf<DecodeInvocation>()`
    - `private val encodeCalls = mutableListOf<EncodeInvocation>()`
    - `@Volatile private var encodeProducer: ((String, Float, Int) -> ShortArray)? = null`
    - `override fun isAvailable(): Boolean = available`
    - `override fun version(): String = version ?: "not loaded"`
    - `override fun decode(samples: ShortArray, sampleRate: Int): Array<Ft8DecodeResult>` — if !available: record invocation with returnedCount=0; return emptyArray(). Else: pop next from decodeQueue (default emptyArray() if empty); record DecodeInvocation; return the array.
    - `override fun encode(message: String, freqHz: Float, sampleRate: Int): ShortArray` — if !available: record EncodeInvocation(message, freqHz, sampleRate, returnedSize=0); return ShortArray(0). Else: invoke encodeProducer if non-null else ShortArray(sampleRate * 15) of zeros; record EncodeInvocation with returnedSize; return.
    - `fun queueDecodeResults(results: List<Ft8DecodeResult>)` — synchronized append to decodeQueue as Array.
    - `fun decodeInvocationsSnapshot(): List<DecodeInvocation>` / `fun encodeInvocationsSnapshot(): List<EncodeInvocation>` — defensive copies.
    - `fun configureIsAvailable(value: Boolean)` / `fun configureVersion(value: String?)` / `fun configureEncodeProducer(producer: ((String, Float, Int) -> ShortArray)?)`.
    - `fun reset()` — clear queue, invocations, encodeCalls; available=true; version="fake-1.0"; encodeProducer=null.

    Also in the same file: `data class DecodeInvocation(val sampleCount: Int, val sampleRate: Int, val returnedCount: Int)` and `data class EncodeInvocation(val message: String, val freqHz: Float, val sampleRate: Int, val returnedSize: Int)`.

    Create ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFakeSelfTest.kt. Package: `net.ft8vc.ft8native.fakes`. JUnit 4 test class `Ft8DecoderFakeSelfTest` with eight `@Test` methods. Imports: `org.junit.Test`, `org.junit.Assert.*`, `net.ft8vc.ft8native.Ft8DecodeResult`. No MockK needed (the fake is plain Kotlin). Construct an Ft8DecoderFake per-test; do not share instances across tests.

    Per CONVENTIONS.md.
  </action>
  <verify>
    <automated>test -f ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt &amp;&amp; test -f ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFakeSelfTest.kt</automated>
    <automated>grep -q 'interface Ft8DecoderApi' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt &amp;&amp; grep -q 'class Ft8DecoderFake : Ft8DecoderApi' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt</automated>
    <automated>grep -q 'fun queueDecodeResults' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt &amp;&amp; grep -q 'fun configureIsAvailable' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt &amp;&amp; grep -q 'fun configureEncodeProducer' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt</automated>
    <automated>grep -q 'data class DecodeInvocation' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt &amp;&amp; grep -q 'data class EncodeInvocation' ft8-native/src/test/java/net/ft8vc/ft8native/fakes/Ft8DecoderFake.kt</automated>
    <automated>./gradlew :ft8-native:test --tests "net.ft8vc.ft8native.fakes.Ft8DecoderFakeSelfTest" --rerun-tasks</automated>
    <automated>! git diff --name-only HEAD ft8-native/src/main/ 2>/dev/null | grep -q . ; echo "no production ft8-native/ changes confirmed"</automated>
  </verify>
  <done>
    - Ft8DecoderFake compiles and implements the Ft8DecoderApi interface.
    - Eight self-test cases pass via `./gradlew :ft8-native:test --tests "*Ft8DecoderFakeSelfTest"`.
    - queueDecodeResults FIFO queue behavior works.
    - configureIsAvailable + configureVersion + configureEncodeProducer all behave as specified.
    - No file under ft8-native/src/main/ was modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fake → production interfaces | Fakes implement production interfaces; if production interface signatures change in future phases, fakes must be updated in lockstep |
| Test classpath only | All fakes live in `src/test/`; never linked into the device APK |
| Fake state across test threads | Tests may inject frames from one thread while the controller-under-test (in a later phase) reads on another |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-06 | Tampering | Fake state under multi-thread access | mitigate | All mutable state on the three fakes is either `@Volatile` (scalar flags) or guarded by `synchronized(historyLock)` / `synchronized(recordsLock)` / `synchronized(queueLock)` for list snapshots. Snapshot accessors return defensive copies via `.toList()` so test assertions cannot mutate fake internals. |
| T-00-07 | Spoofing | Interface drift | mitigate | The two fakes that implement production interfaces (FakeRigBackend ↔ RigBackend+CatControl; FakeUsbAudioCapture ↔ AudioEngine) compile against the production interfaces directly — any signature drift breaks Phase 0's tests immediately. The third (FakeUsbAudioPlayback) and Ft8DecoderFake define a parallel interface in test source because production exposes concrete classes only; Phase 3/5 plans will introduce the parallel interface in production source and bridge it. |
| T-00-08 | Information Disclosure | Fake invocation history | accept | DecodeInvocation/EncodeInvocation/PttEdge/PlaybackRecord may contain raw FT8 messages and PCM payloads from test fixtures. These never leave the test JVM. |
| T-00-SC | Tampering | Supply chain | accept | Plan 02 already declared and resolved Turbine + MockK at pinned versions. This plan adds no new package installs. |
</threat_model>

<verification>
- All seven new files exist in their canonical `src/test/.../fakes/` packages.
- `./gradlew :rig:test :audio:test :ft8-native:test --rerun-tasks` exits 0.
- No file under `*/src/main/` was modified (git diff sanity check above).
- Each fake mirrors the surface of the production sibling it stands in for.
- Failure-injection switches (`simulateDetach`, `configureTimeoutHz`, `configureZeroSampleMode`, `simulateDeviceRemoved`, `configureBlockingMs`, `configureFailFastReturn`, `configureIsAvailable`, `configureVersion`) all have positive self-test coverage.
</verification>

<success_criteria>
- FakeRigBackend, FakeUsbAudioCapture, FakeUsbAudioPlayback, Ft8DecoderFake all compile under their modules' test source sets.
- Three self-test classes pass.
- Each fake exports the failure switches enumerated in REQUIREMENTS.md FOUND-03/04/05.
- No production code under rig/, audio/, ft8-native/, core/, app/, or data/ src/main/ is modified.
- Plan 04 can `import net.ft8vc.rig.fakes.FakeRigBackend`, `import net.ft8vc.audio.fakes.FakeUsbAudioCapture`, etc., from the golden-trace harness.
</success_criteria>

<output>
Create `.planning/phases/00-foundations/00-03-SUMMARY.md` when done. Summary must include: (a) list of files created with line counts, (b) which production interfaces each fake implements vs which expose a parallel test-side interface (for Phase 3/5 to bridge later), (c) inventory of failure-injection switches per fake, (d) self-test results (8 + 8 + 8 = 24 tests passing), (e) requirement IDs satisfied (FOUND-03, FOUND-04, FOUND-05), (f) explicit confirmation no production source was modified.
</output>
