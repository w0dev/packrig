package net.packrig.core

/** Operate-screen decode list view mode (display only). */
enum class DecodeViewMode {
    /** Every decode in the passband. */
    ALL,

    /** CQ, traffic directed to you, and active QSO partner. */
    OPERATE,
}
